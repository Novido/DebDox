fastapi[standard]
uvicorn[standard]
pydantic
pydantic-settings
python-jose[cryptography]
bcrypt
python-multipart
libvirt-python
docker
websockets
aiofiles
aiosqlite
sqlalchemy[asyncio]
# Note: borg is invoked as a CLI (see services/backup_service.py); the
# borgbackup *package* is installed system-wide via apt, so it is NOT a
# pip dependency here (avoids compiling its C extensions in the venv).
