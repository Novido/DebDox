# Debian preseed for DebDox bare-metal installation
# Used by the TUI installer (whiptail-based) after boot

# Locale / keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select se

# Network (configured by TUI installer post-preseed)
d-i netcfg/get_hostname string debdox
d-i netcfg/get_domain string local

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Clock
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Stockholm
d-i clock-setup/ntp boolean true

# Partitioning (TUI installer handles ZFS setup separately)
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Root password disabled — only debdox admin account
d-i passwd/root-login boolean false
d-i passwd/user-fullname string DebDox Admin
d-i passwd/username string admin
d-i passwd/user-password-crypted password CHANGEME_CRYPTED_PW

# Base system
d-i base-installer/install-recommends boolean false

# Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Finish
d-i finish-install/reboot_in_progress note
